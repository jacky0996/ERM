
#user  nobody;
worker_processes 1;

# 將錯誤日誌導向標準錯誤輸出，方便 docker logs 查看
error_log /dev/stderr warn;
pid        /tmp/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  # 將存取日誌導向標準輸出
  access_log /dev/stdout;

  # 如果 mime.types 沒包含 mjs，額外補充在此
  types {
    application/javascript mjs;
  }

  sendfile on;
  # tcp_nopush     on;

  #keepalive_timeout  0;
  # keepalive_timeout 65;

  # gzip on;
  # gzip_buffers 32 16k;
  # gzip_comp_level 6;
  # gzip_min_length 1k;
  # gzip_static on;
  # gzip_types text/plain
  #   text/css
  #   application/javascript
  #   application/json
  #   application/x-javascript
  #   text/xml
  #   application/xml
  #   application/xml+rss
  #   text/javascript; #设置压缩的文件类型
  # gzip_vary on;

  server {
    listen 80;
    server_name uatedm.hwacom.com;

    location / {
      root /usr/share/nginx/html;
      try_files $uri $uri/ /index.html;
      index index.html;
      # 暫時開啟 CORS 以便透過 IP 直接進行除錯
      add_header 'Access-Control-Allow-Origin' '*';
      add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
      add_header 'Access-Control-Allow-Headers' 'DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
    }

    # === [資安隱藏] 反向代理業務 API，隱藏後端實體域名 ===
    location /api/ {
      # 本地開發: 透過 docker network 直接連到後端 nginx container
      proxy_pass http://edm-nginx/api/;

      proxy_set_header Host edm-nginx;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # === [資安隱藏] 反向代理 SSO 驗證 (使用內網 IP 避開防火牆) ===
    location /api-sso/ {
      proxy_pass http://192.168.3.5/api/;
      
      proxy_set_header Host uathws.hwacom.com;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    error_page 500 502 503 504 /50x.html;

    location = /50x.html {
        root /usr/share/nginx/html;
    }
  }
}
